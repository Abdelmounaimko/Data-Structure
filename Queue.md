# 📚 Queue Implementation in C++ (Using Linked List)

This project implements a **Queue (FIFO – First In, First Out)** using a **singly linked list** in C++.  
It includes the essential queue operations:

- **EnQueue** → Add an element to the queue  
- **DeQueue** → Remove an element from the queue  
- **Queue_Display** → Show all elements  
- **Exit** → End the program  

---

## 🚀 Features

### ✔ EnQueue  
Adds a new node to the **rear** of the queue.

### ✔ DeQueue  
Removes a node from the **front** of the queue.  
Shows a message if the queue is empty.

### ✔ Queue_Display  
Displays all elements from **front to rear**.

### ✔ User Menu  
The program provides an interactive console menu for testing queue operations.

---

## 🛠 How to Compile & Run

### Compile:
```bash
g++ Queue.cpp -o queue
